package main.enums;

public enum TokenType {
    MOP, INT, ID, ADDOP, ASSIGN, RELOP, NEGATE, LB, RB, COMMA, SEMI,
    PERIOD, LP, RP, CONST, IF, GET, PRINT, VAR, THEN, PROC, WHILE, CALL, DO, ODD, CLASS
}
