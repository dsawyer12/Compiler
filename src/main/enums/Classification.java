package main.enums;

    public enum Classification {
        $, PGM, BLOCK, C, CONST_LIST, V, VAR_LIST, I, ID_LIST, STMT, SS, CALL_STMT, CMPD_STMT, IF_STMT, WHILE_STMT, S, STMT_LIST, B_E, E, EXP,
        T, TERM, FAC, CLASS, CONST, VAR, PROC, CALL, IF, THEN, WHILE, DO, ODD, LB, RB, SEMI, LP, RP, COMMA, ASSIGN, ADDOP, RELOP, MOP, ID, INT
    }
